.venv/
build/
dist/
*.pyc
__pycache__/
.DS_Store
.vscode/.vscode
.env
.eggs/
*.egg-info/

# ESP-IDF managed components (regenerated by idf-component-manager from idf_component.yml)
managed_components/

# Locally cloned QEMU source (built via tools/build-qemu.sh)
tools/qemu-src/

# Playwright/CDP test outputs (regenerated each run)
artifacts/

# GitHub Copilot CLI tooling (Day 2 dual-CLI parity work) — installed via
# `npm install @github/copilot`; npm-managed, regenerable, not source-of-truth.
node_modules/
package.json
package-lock.json

# ESP-IDF leaves this behind after `idf.py menuconfig` / `reconfigure`.
sdkconfig.old
